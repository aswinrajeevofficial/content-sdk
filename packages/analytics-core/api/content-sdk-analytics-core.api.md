## API Report File for "@sitecore-content-sdk/analytics-core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { IncomingMessage } from 'http';
import type { OutgoingMessage } from 'http';
import { Plugin as Plugin_2 } from '@sitecore-content-sdk/core';
import { PluginAdapter } from '@sitecore-content-sdk/core';

// @public
export const ANALYTICS_PLUGIN_NAME = "AnalyticsPlugin";

// @public
export interface AnalyticsAdapter extends PluginAdapter {
    getClientId: () => string | null;
    location: {
        getSearchParams: () => string;
    };
    setClientId: () => Promise<void>;
}

// @public
export interface AnalyticsBrowserAdapter extends AnalyticsAdapter {
    type: 'browser';
}

// @public
export function analyticsBrowserAdapter(): AnalyticsBrowserAdapter;

// @public
export interface AnalyticsOptions {
    cookies: {
        name: string;
        domain?: string;
        expiryDays: number;
        path?: string;
        enabled?: boolean;
    };
    timeout?: number;
    visitorIds?: VisitorIds;
}

// @public
export interface AnalyticsPlugin extends Plugin_2 {
    adapter: AnalyticsAdapter;
    init: () => Promise<void>;
    name: typeof ANALYTICS_PLUGIN_NAME;
    options: AnalyticsOptions;
}

// @public
export function analyticsPlugin(params: AnalyticsPluginParams): AnalyticsPlugin;

// @public
export interface AnalyticsPluginParams {
    adapter: AnalyticsAdapter;
    options?: {
        cookieDomain?: string;
        cookieExpiryDays?: number;
        cookiePath?: string;
        enableCookie?: boolean;
        timeout?: number;
    };
}

// @public
export interface AnalyticsServerAdapter extends AnalyticsAdapter {
    type: 'server';
}

// @public
export function analyticsServerAdapter<Request extends IncomingMessage, Response extends OutgoingMessage>(request: Request, response: Response): AnalyticsServerAdapter;

// @internal
export const API_VERSION = "v1.2";

// @internal
export function appendScriptWithAttributes(attributes: ScriptAttributes): void;

// @public
export type BasicTypes = string | boolean | number | undefined | Array<string | boolean | number | {
    [key: string]: BasicTypes;
} | Array<BasicTypes>>;

// @internal
export const CLIENT_ID_COOKIE_NAME = "cid";

// @internal
export interface Cookie {
    // (undocumented)
    name: string;
    // (undocumented)
    value: string;
}

// @internal
export const COOKIE_NAME_PREFIX = "sc_";

// @internal
export function cookieExists(cookieStr: string, cookieName: string): boolean;

// @internal
export interface CookieProperties {
    // (undocumented)
    domain?: string;
    // (undocumented)
    expires?: Date;
    // (undocumented)
    httpOnly?: boolean;
    // (undocumented)
    maxAge: number;
    // (undocumented)
    path?: string;
    // (undocumented)
    sameSite: string;
    // (undocumented)
    secure: boolean;
}

// @internal
export const CORRELATION_ID_HEADER = "x-sc-correlation-id";

// @internal
export function createCookieString(name: string, value: string, attributes: CookieProperties): string;

// @internal
export const DAILY_SECONDS = 86400;

// @internal
export const DEFAULT_COOKIE_EXPIRY_DAYS = 730;

// @public
export interface EPResponse {
    // (undocumented)
    client_key: string;
    // (undocumented)
    customer_ref: string;
    // (undocumented)
    ref: string;
    // (undocumented)
    status: string;
    // (undocumented)
    version: string;
}

// @internal
export function fetchClientIdFromEdgeProxy(edgeUrl: string, contextId: string, timeout?: number): Promise<VisitorIds>;

// @internal
export interface FlattenedObject {
    // (undocumented)
    [key: string]: BasicTypes;
}

// @internal
export function flattenObject(data: FlattenObjectDataParameters): FlattenedObject;

// @internal
export interface FlattenObjectDataParameters {
    // (undocumented)
    currentKey?: string;
    // (undocumented)
    newObject?: FlattenedObject;
    // (undocumented)
    object: NestedObject;
}

// @internal
export function generateCorrelationId(): string;

// @internal
export function generateV4UUID(): string;

// @internal
export function getAnalyticsPlugin(): AnalyticsPlugin;

// @public
export function getClientId(): string;

// @internal
export function getCookie(cookieStr: string | undefined, cookieName: string): {
    name: string;
    value: string;
} | undefined;

// @internal
export function getCookieServerSide(cookiesHeader: string | undefined, cookieName: string): {
    name: string;
    value: string;
} | undefined;

// @internal
export function getCookieValueClientSide(cookieName: string): string;

// @internal
export function getDefaultCookieAttributes(maxAge?: number, cookieDomain?: string): CookieProperties;

// @internal
export interface Infer {
    // (undocumented)
    language: () => string | undefined;
    // (undocumented)
    pageName: () => string;
}

// @internal
export function isShortISODateString(date: string): boolean;

// @internal
export function isValidEmail(email: string): boolean;

// @internal
export function language(): string | undefined;

// @internal
export const LIBRARY_VERSION: string;

// @public
export interface NestedObject {
    [key: string]: BasicTypes | NestedObject;
}

// @internal
export function pageName(): string;

// @internal
export interface ScriptAttributes {
    async: boolean;
    src: string;
}

// @public
export interface VisitorIds {
    clientId: string;
    profileId: string;
}

// (No @packageDocumentation comment for this package)

```
